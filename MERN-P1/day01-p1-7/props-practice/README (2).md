## 🧱 Layout Project

### 🧰 **Project Name**: Simple Page Layout with Reusable Component

---

### 🎯 Goal:

Build a **reusable `Layout` component** that:

- Takes a **page title** via props
- Uses **`children`** to render different main content
- Includes a **header**, **main section**, and **footer**
